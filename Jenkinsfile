#!/usr/bin/env groovy

/*
 * Jenkinsfile for testing Sumo Logic plugin
 */
pipeline {
    agent any 
    stages {
        stage('Build') {
            steps {
                echo 'Hello world!' 
            }
        }
    }
}